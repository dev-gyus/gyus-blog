spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  data:
    web:
      pageable:
        one-indexed-parameters: false
  datasource:
    url: jdbc:h2:mem:gyublog
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
